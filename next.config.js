/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['down-id.img.susercontent.com'], // Tambahin domain yang kamu pakai buat gambar
  },
};

export default nextConfig;
